# QA v1.0

Проверено перед упаковкой:

- синтаксис `app.js`, `patch-v08.js`, `patch-v09.js`, `patch-v10.js`;
- загрузка 13 JSON-файлов;
- 42 карточки имеют непустой `image.file`;
- все 15 архивных карт v0.9 получили новые проверенные имена файлов;
- `index.html` подключает `patch-v10.js` после v0.9;
- Архив строится только из `isUnlocked(card.id)`;
- Коллекция строится из полного массива `CARDS`;
- закрытое досье не раскрывает факты, связи и задания;
- старые поля сохранения не удаляются, новые поля имеют значения по умолчанию.
