# QA v1.1

Проверено перед упаковкой:

- 42 карточки загружаются из трёх JSON-паков;
- 42 связи имеют существующие source и target;
- 7 миссий ссылаются на существующие карточки и квизы;
- 7 архивных пулов имеют валидные карточки и условия открытия;
- 6 личных историй привязаны к существующим персонажам;
- все JS-файлы проходят `node --check`;
- runtime smoke-test рендерит главную, архив, полный каталог и кампанию;
- миграционный smoke-test сохраняет XP, тему, карты, миссии, квизы и фрагменты v1.0;
- валидатор завершён без ошибок;
- старый ключ localStorage не изменён;
- GitHub Pages не требует сборки или backend.

Headless Chromium в контейнере не завершил системный запуск из-за ограничений окружения, поэтому UI дополнительно проверен через изолированный runtime smoke-test.
