# QA v1.3

Проверено:

- версия 1.3.0 отображается в desktop, mobile и настройках;
- экран настроек рендерится;
- compact/comfortable density применяется через `data-density`;
- размер текста и reduced motion сохраняются;
- принудительное обновление создаёт cache-busting token;
- прогресс хранится отдельно от настроек;
- ежедневная сессия v1.2 продолжает работать;
- контент-валидатор проходит;
- JavaScript проходит синтаксическую проверку.
