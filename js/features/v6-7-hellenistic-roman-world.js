/* Codex v8.0.0 — Hellenistic and Roman world shared comparative layer */
(()=>{
  const V='8.0.0';window.CODEX_VERSION=V;V22_CAMPAIGN_CODES.HELLENISTIC_ROMAN_EXAM='HELLENISTIC_ROMAN_EXAM';state.ancientWorldRegion=state.ancientWorldRegion||'HELLENISTIC';
  const oldSync=syncActiveCampaignRuntime;syncActiveCampaignRuntime=function(){oldSync();if(activeCampaignId()!=='HELLENISTIC_ROMAN_EXAM')return;const first=CAMPAIGN.nodes[0];(first?.cards||[]).forEach(id=>{if(!state.unlocked.includes(id))state.unlocked.push(id);});CODEX_CONFIG.maps=CODEX_MAPS.HELLENISTIC_ROMAN_EXAM||CODEX_CONFIG.maps;};
  const oldNoun=activeCampaignNoun;activeCampaignNoun=function(){return activeCampaignId()==='HELLENISTIC_ROMAN_EXAM'?'ЭПОХА V · АНТИЧНЫЙ МИР':oldNoun();};
  const oldPackTitle=activeCampaignPackTitle;activeCampaignPackTitle=function(){return activeCampaignId()==='HELLENISTIC_ROMAN_EXAM'?'Общий архив античного мира':oldPackTitle();};
  const oldPackCover=activeCampaignPackCover;activeCampaignPackCover=function(){return activeCampaignId()==='HELLENISTIC_ROMAN_EXAM'?'assets/packs/hellenistic-roman-era-pack.svg':oldPackCover();};
  const layer=()=>CODEX_CAMPAIGNS.HELLENISTIC_ROMAN_EXAM?.eraLayer||{};const regionProgress=r=>typeof campaignProgressFor==='function'?campaignProgressFor(r.campaignId):0;
  window.focusAncientWorldRegion=function(id){const r=(layer().regions||[]).find(x=>x.id===id);if(!r)return;state.ancientWorldRegion=id;save();focusMapPoint(r.center,r.zoom||4,`<div class="map-popup dawn-popup"><b>${esc(r.title)}</b><span>${esc(r.summary)}</span><small>Прогресс кампании: ${regionProgress(r)}%</small><button onclick="startWorldCampaign('${r.campaignId}')">Открыть кампанию</button></div>`);document.querySelectorAll('[data-ancient-region]').forEach(el=>el.classList.toggle('active',el.dataset.ancientRegion===id));};
  function regionStrip(){const icons={HELLENISTIC:'Δ',ROME:'SPQR',MAURYA:'☸',HAN:'漢',STEPPE:'↟'};return `<section class="dawn-region-strip bronze-region-strip reveal"><div class="section-head"><div><small>КАРТА ПЯТОЙ ЭПОХИ</small><h2>Пять связанных миров</h2></div><span>нажми, чтобы приблизить</span></div><div class="dawn-region-grid classical-region-grid">${(layer().regions||[]).map(r=>`<button data-ancient-region="${r.id}" class="${state.ancientWorldRegion===r.id?'active':''}" onclick="focusAncientWorldRegion('${r.id}')"><span>${icons[r.id]||'◇'}</span><div><b>${r.title}</b><small>${regionProgress(r)}% · ${r.summary}</small></div></button>`).join('')}</div></section>`;}
  function parallelTimeline(){return `<section class="parallel-era-timeline bronze-parallel-timeline reveal"><div class="section-head"><div><small>ПАРАЛЛЕЛЬНАЯ ХРОНОЛОГИЯ</small><h2>Пять регионов на одной шкале</h2></div><span>совпадение дат не доказывает прямую связь</span></div><div class="parallel-table-wrap"><table><thead><tr><th>Период</th><th>Эллинистический мир</th><th>Рим</th><th>Маурьи и Южная Азия</th><th>Хань</th><th>Степь и оазисы</th></tr></thead><tbody>${(layer().parallelTimeline||[]).map(x=>`<tr><th><b>${x.date}</b><small>${x.certainty}</small></th><td>${x.HELLENISTIC}</td><td>${x.ROME}</td><td>${x.MAURYA}</td><td>${x.HAN}</td><td>${x.STEPPE}</td></tr>`).join('')}</tbody></table></div></section>`;}
  const oldHome=home;home=function(){syncActiveCampaignRuntime();if(activeCampaignId()!=='HELLENISTIC_ROMAN_EXAM')return oldHome();const m=currentMission(),ch=chapterForMission(m.id),owned=state.unlocked.filter(id=>card(id)?.campaign==='HELLENISTIC_ROMAN_EXAM').length;return shell(`<section class="home-hero home-hero-clean bronze-world-home reveal"><div class="hero-layout"><div class="hero-content"><div class="eyebrow">Античный мир · итог пятой эпохи</div><h2>${m.title}</h2><p>${ch.subtitle}</p><div class="hero-actions"><button class="btn" onclick="openMission('${m.id}')">${m.emoji} Продолжить</button><button class="btn secondary" onclick="go('campaign')">Все миссии</button></div></div><div class="collapse-seal"><span>∷</span><b>323 до н. э.</b><small>220 год н. э.</small></div></div></section><section class="home-stats reveal"><div class="stat-box"><div class="stat-icon">◎</div><b>${campaignProgress()}%</b><span>кампания</span></div><div class="stat-box"><div class="stat-icon">▤</div><b>${completedMissionCount()}/${CAMPAIGN.nodes.length}</b><span>миссий</span></div><div class="stat-box"><div class="stat-icon">▦</div><b>${owned}/96</b><span>карточек</span></div><div class="stat-box stat-action" onclick="openPackHub()"><div class="stat-icon">✦</div><b>${dailyPackStatusShort()}</b><span>пак дня</span></div></section>${regionStrip()}<section class="daily-home-card reveal" onclick="openDaily()"><div class="daily-home-icon">◷</div><div><div class="eyebrow">Ежедневное обучение</div><h3>${dailyLearningCompleteToday()?'Сессия выполнена':`К повторению: ${dailyDueCards().length}`}</h3><p>${dailyLearningCompleteToday()?'Пак дня доступен.':'Повтори открытые знания за несколько минут.'}</p></div><button class="btn ${dailyLearningCompleteToday()?'secondary':''}">${dailyLearningCompleteToday()?'Открыть':'Начать'}</button></section>`);};
  const oldCampaign=campaign;campaign=function(){let html=oldCampaign();if(activeCampaignId()!=='HELLENISTIC_ROMAN_EXAM')return html;html=html.replace(/РИМСКАЯ КАМПАНИЯ|МЕСОПОТАМСКАЯ КАМПАНИЯ|ЕГИПЕТСКАЯ КАМПАНИЯ|КАМПАНИЯ БРОНЗОВОГО ВЕКА|ЖЕЛЕЗНЫЙ ВЕК[^<]*|КЛАССИЧЕСКИЙ МИР[^<]*|ЭЛЛИНИСТИЧЕСКИЙ И РИМСКИЙ МИР[^<]*/g,'ОБЩИЙ ЭКЗАМЕН ПЯТОЙ ЭПОХИ');return html.replace('</div></main><nav',`${parallelTimeline()}</div></main><nav`);};
  const oldPacks=packsScreen;packsScreen=function(){if(activeCampaignId()!=='HELLENISTIC_ROMAN_EXAM')return oldPacks();const pools=unlockedPools();return shell(`<section class="packs-page-head reveal"><div class="packs-title-block"><div class="eyebrow">Архив · Античный мир</div><h2>Общий архив эпохи</h2><p>Архивные карточки открываются по мере прохождения сравнительных глав.</p><div class="fragment-balance compact-fragment-balance"><span>◇</span><b>${state.fragments}</b><small>фрагментов</small></div></div></section><section class="packs-page-grid reveal"><article class="pack-page-card ${dailyPackReady()?'ready':''}"><img src="${packCover('DAILY')}" alt="Архивный пак дня"><div class="pack-page-copy"><small>ЕЖЕДНЕВНЫЙ · 3 КАРТЫ</small><h3>Архивный пак дня</h3><p>${dailyPackReady()?'Готов к открытию.':dailyLearningCompleteToday()?'Сегодня уже открыт.':'Сначала заверши дневную сессию.'}</p>${packAction('DAILY')}</div></article><article class="pack-page-card campaign-pack ${campaignPackStatusClass()}"><img src="assets/packs/hellenistic-roman-era-pack.svg" alt="Общий архив античного мира"><div class="pack-page-copy"><small>ПЯТАЯ ЭПОХА · 4 КАРТЫ</small><h3>Общий архив античного мира</h3><p>${campaignPackDescription()}</p><div class="pack-page-meta">${campaignPackMeta()}</div>${campaignPackAction()}</div></article></section>${pools.length?`<section class="section reveal"><div class="section-head"><h2>Открытые пулы</h2><span>${pools.length}</span></div><div class="active-pools compact-pools">${pools.map(p=>{const pr=poolProgress(p);return `<span>${p.title} ${pr.opened}/${pr.total}</span>`;}).join('')}</div></section>`:''}`);};
  const oldMap=mapScreen;mapScreen=function(){if(activeCampaignId()!=='HELLENISTIC_ROMAN_EXAM')return oldMap();const opened=[...new Set(CAMPAIGN.nodes.flatMap(n=>n.cards||[]))].map(card).filter(c=>c&&isUnlocked(c.id));return shell(`<section class="collection-header compact-collection-head reveal"><div><div class="eyebrow">Глобальная карта пятой эпохи</div><h2>Средиземноморье, Индия, Китай и степь</h2></div><div class="collection-count">${opened.length} точек</div></section>${regionStrip()}<div class="map-shell atlas-clean dawn-atlas reveal"><div id="atlas-map" class="leaflet-map"></div><div class="map-actions"><button class="map-action" onclick="resetAtlasView()" title="Общий обзор">⌂</button><button class="map-action" onclick="fitAtlasMarkers()" title="Показать точки">◎</button></div></div>${parallelTimeline()}<section class="section compact-section reveal"><div class="section-head"><h2>Открытые точки</h2><span>${opened.length}</span></div><div class="location-grid">${opened.map(c=>`<article class="panel panel-click location-card" onclick="focusAtlasCard('${c.id}')"><small>${c.region}</small><h3>${typeIcon(c.type)} ${c.title}</h3><p>${c.date}</p></article>`).join('')}</div></section>`);};
  window.openAncientWorldExamModule=function(quizId,missionId){state.ancientWorldExamCurrent=quizId;markLessonCheck(missionId,true);save();openQuiz(quizId,missionId);};
  const oldActivity=lessonActivity;lessonActivity=function(m,l){if(!String(m?.id||'').startsWith('HRE_')||!m.examModules)return oldActivity(m,l);const modules=m.examModules||[],passed=modules.filter(x=>isQuizPassed(x.id)).length,all=passed===modules.length;const exam=`<div class="era-exam assyria-exam"><header><small>ИТОГ ЭПОХИ</small><h3>${all?'Экзамен завершён':`${passed}/${modules.length} модулей`}</h3><p>Хронология, власть, армии, сети, культура и исторический метод проверяются отдельно.</p><div class="progress"><span style="width:${Math.round(passed/Math.max(1,modules.length)*100)}%"></span></div></header><div class="era-exam-grid">${modules.map((x,i)=>{const r=quizResult(x.id),done=isQuizPassed(x.id);return `<article class="${done?'done':''}"><span>${done?'✓':String(i+1).padStart(2,'0')}</span><div><b>${x.title}</b><small>${r?`лучший результат ${r.bestPercent}%`:'5 вопросов'}</small></div><button class="btn ${done?'secondary':''}" onclick="openAncientWorldExamModule('${x.id}','${m.id}')">${done?'Повторить':'Начать'}</button></article>`;}).join('')}</div>${all?`<button class="btn" onclick="if(!missionCompleted('${m.id}'))completeMission('${m.id}')">Завершить эпоху</button>`:''}</div>`;return `<section class="lesson-stage learning-practice"><div class="lesson-stage-head"><div><small>ЭТАП 5</small><h2>Практика и закрепление</h2></div><span class="lesson-stage-number">05</span></div>${exam}<div class="learning-next"><button class="btn ghost" onclick="setLessonStage('${m.id}',3)">← Вернуться к теории</button></div></section>`;};
  syncActiveCampaignRuntime();save();
})();
