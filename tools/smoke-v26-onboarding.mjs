#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const read=p=>fs.readFileSync(path.join(root,p),'utf8');const json=p=>JSON.parse(read(p));const fail=m=>{console.error('FAIL',m);process.exit(1)};
const manifest=json('data/content-manifest.json');const script=read('js/features/v2-6-onboarding.js');const css=read('styles.css');const settings=read('js/features/settings.js');
if(manifest.version!=='2.6.0')fail('version');if(!manifest.scripts.includes('js/features/v2-6-onboarding.js'))fail('script manifest');if(!script.includes("title:'Выбери эпоху'")||!script.includes("title:'Выбери первую кампанию'"))fail('choice steps');if(!script.includes('finishOnboarding'))fail('finish');if(!script.includes('replayOnboarding'))fail('replay');if(!css.includes('.onboarding-v26'))fail('css');if(!script.includes('Рассказ')||!script.includes('Хронология')||!script.includes('Теория')||!script.includes('Практика'))fail('learning explainer');console.log('✓ v2.6 onboarding: 7 steps, era and campaign selection, replay');
