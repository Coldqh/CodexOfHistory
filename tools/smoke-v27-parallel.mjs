#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const j=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));const fail=m=>{console.error('FAIL',m);process.exit(1)};
const m=j('data/content-manifest.json'),cs=j('data/campaigns/comparison/campaign.json'),cards=[...j('data/cards/comparison/story.json'),...j('data/cards/comparison/archive.json')],world=j('data/world/campaigns.json'),on=fs.readFileSync(path.join(root,'js/features/v2-6-onboarding.js'),'utf8');
if(m.version!=='2.7.0')fail('version');if(cs.chapters.length!==6||cs.nodes.length!==24)fail('campaign');if(cards.length!==36)fail('cards');if(!world.some(x=>x.id==='DAWN_PARALLEL'&&x.status==='PLAYABLE'))fail('world');if(on.includes('ты последовательно собираешь картину прошлого'))fail('old AI copy');if(!on.includes('В Codex история разбита на эпохи'))fail('new onboarding copy');for(const id of cs.nodes.map(x=>x.id)){const lessons=j('data/lessons/comparison/campaign.json');if(!lessons[id]?.theory?.paragraphs?.length)fail('lesson '+id)}console.log('✓ v2.7 parallel civilizations and onboarding copy');