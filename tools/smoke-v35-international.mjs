#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const read=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));const exists=p=>fs.existsSync(path.join(root,p));
const manifest=read('data/content-manifest.json');assert.equal(manifest.version,'6.0.0');
for(const p of ['data/cards/international-bronze/story.json','data/cards/international-bronze/archive.json','data/campaigns/international-bronze/campaign.json','data/campaigns/international-bronze/pools.json','data/lessons/international-bronze/campaign.json','data/quizzes/international-bronze/campaign.json','data/stories/international-bronze/personal.json','data/maps/international-bronze.json','assets/packs/international-bronze-pack.svg','js/features/v3-5-international.js'])assert.ok(exists(p),`missing ${p}`);
const story=read('data/cards/international-bronze/story.json'),archive=read('data/cards/international-bronze/archive.json'),campaign=read('data/campaigns/international-bronze/campaign.json'),pools=read('data/campaigns/international-bronze/pools.json'),lessons=read('data/lessons/international-bronze/campaign.json'),quizzes=read('data/quizzes/international-bronze/campaign.json'),stories=read('data/stories/international-bronze/personal.json'),map=read('data/maps/international-bronze.json');
assert.equal(story.length,80);assert.equal(archive.length,40);assert.equal(campaign.chapters.length,10);assert.equal(campaign.nodes.length,60);assert.equal(Object.keys(lessons).length,60);assert.equal(Object.keys(quizzes).length,14);assert.equal(pools.pools.length,10);assert.equal(Object.keys(stories).length,10);
assert.equal(campaign.nodes[0].id,'INT_01_01');assert.equal(campaign.nodes.at(-1).campaignExamModules.length,4);assert.equal(campaign.nodes.at(-1).id,'INT_10_06');
assert.ok(map.points.AMARNA);assert.ok(map.points.UGARIT);assert.ok(map.points.CYPRUS);assert.ok(map.points.ULUBURUN);assert.equal(Object.keys(map.chapters).length,10);
for(const c of [...story,...archive]){assert.ok(exists(c.image.local),`${c.id}: missing image`);assert.equal(c.campaign,'BRONZE_INTERNATIONAL');}
assert.ok(archive.every(c=>c.acquisition==='ARCHIVE'));assert.ok(story.every(c=>c.acquisition==='STORY'));
const world=read('data/world/campaigns.json').find(c=>c.id==='BRONZE_INTERNATIONAL');assert.equal(world.status,'PLAYABLE');assert.equal(world.releasedChapters,10);assert.equal(world.chapterCount,10);
const queries=read('data/image_queries.json');assert.equal(queries.count,2987);assert.equal(queries.cards.INT_S_02_01.candidates[0].title,'Amarna letters');assert.equal(queries.cards.INT_S_07_01.candidates[0].title,'Ugarit');assert.equal(queries.cards.INT_S_08_01.candidates[0].title,'Alashiya');assert.equal(queries.cards.INT_S_09_01.candidates[0].title,'Uluburun shipwreck');
const rel=read('data/core/relations.json');assert.ok(rel.some(r=>r.source==='EMN_S_09_05'&&r.target==='INT_S_02_01'));assert.ok(rel.some(r=>r.source==='AEG_S_09_05'&&r.target==='INT_S_09_01'));
const runtime=fs.readFileSync(path.join(root,'js/features/v3-5-international.js'),'utf8');for(const token of ["const V='6.0.0'",'BRONZE_INTERNATIONAL','international-phases','openInternationalExamModule','assets/packs/international-bronze-pack.svg'])assert.match(runtime,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));
console.log('✓ v3.5 international Bronze Age static smoke: 10 chapters, 60 missions, 120 cards');
