#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const read=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));const exists=p=>fs.existsSync(path.join(root,p));
const manifest=read('data/content-manifest.json');assert.equal(manifest.version,'6.9.1');
const required=['data/cards/iron-world/story.json','data/cards/iron-world/archive.json','data/campaigns/iron-world/campaign.json','data/campaigns/iron-world/pools.json','data/lessons/iron-world/campaign.json','data/quizzes/iron-world/campaign.json','data/stories/iron-world/personal.json','data/maps/iron-world-exam.json','assets/packs/iron-era-pack.svg','js/features/v4-6-iron-world.js'];for(const p of required)assert.ok(exists(p),`missing ${p}`);
const story=read('data/cards/iron-world/story.json'),archive=read('data/cards/iron-world/archive.json'),campaign=read('data/campaigns/iron-world/campaign.json'),pools=read('data/campaigns/iron-world/pools.json'),lessons=read('data/lessons/iron-world/campaign.json'),quizzes=read('data/quizzes/iron-world/campaign.json'),stories=read('data/stories/iron-world/personal.json'),map=read('data/maps/iron-world-exam.json');
assert.equal(story.length,72);assert.equal(archive.length,24);assert.equal(campaign.chapters.length,8);assert.equal(campaign.nodes.length,48);assert.equal(Object.keys(lessons).length,48);assert.equal(Object.keys(quizzes).length,14);assert.equal(pools.pools.length,8);assert.equal(Object.keys(stories).length,8);assert.equal(campaign.nodes.at(-1).examModules.length,6);assert.equal(campaign.eraLayer.regions.length,6);assert.equal(campaign.eraLayer.parallelTimeline.length,5);
for(const c of [...story,...archive]){assert.ok(exists(c.image.local),`${c.id}: missing image`);assert.equal(c.campaign,'IRON_WORLD');}assert.ok(story.every(c=>c.acquisition==='STORY'));assert.ok(archive.every(c=>c.acquisition==='ARCHIVE'));
const world=read('data/world/campaigns.json').find(c=>c.id==='IRON_ERA_EXAM');assert.equal(world.status,'PLAYABLE');assert.equal(world.chapterCount,8);const era=read('data/world/eras.json').find(e=>e.id==='ERA_IRON');assert.ok(era.campaignIds.includes('IRON_ERA_EXAM'));
const queries=read('data/image_queries.json');assert.equal(queries.version,'6.9.1');assert.equal(queries.count,3843);assert.equal(Object.keys(queries.cards).length,3843);assert.equal(queries.cards.IRN_S_01_01.group,'IRON_WORLD');const images=read('data/image_manifest.json');assert.equal(images.count,3843);assert.equal(images.projectCoverCount,3801);
const runtime=fs.readFileSync(path.join(root,'js/features/v4-6-iron-world.js'),'utf8');for(const token of ["const V='6.9.1'",'IRON_ERA_EXAM','focusIronWorldRegion','openIronExamModule','assets/packs/iron-era-pack.svg'])assert.ok(runtime.includes(token),`missing ${token}`);
console.log('✓ v4.6 Iron Age shared layer static smoke: 8 chapters, 48 missions, 96 cards');
